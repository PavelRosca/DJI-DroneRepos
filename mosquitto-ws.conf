allow_anonymous true

# MQTT from localhost is not required; we only use websocket for RC pilots.
# Explicit bind prevents localhost-only binding in some Windows installs.
listener 8083 0.0.0.0
protocol websockets
allow_anonymous true
